pre-push:
  parallel: true
  commands:
    lint:
      tags: run linter
      run: golangci-lint run --fix
      glob: "*.go"
    test:
      tags: run tests
      run: go test -v -race ./...
      glob: "*.go"